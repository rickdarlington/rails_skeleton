source 'https://rubygems.org'

#stuff you might want to use some time:
#gem 'kaminari'		#pagination for activerecord items
#gem 'doorkeeper'	#oauth2 provider
# gem 'bcrypt', 	#secure activemodel passwords

gem 'rails', '4.2.2'

# Use sqlite3 as the database for Active Record
#gem 'sqlite3'

gem 'pg'

# Use SCSS for stylesheets
gem 'sass-rails'
gem 'bootstrap-sass'
gem 'autoprefixer-rails'

# Use Uglifier as compressor for JavaScript assets
gem 'uglifier', '>= 1.3.0'

# Use CoffeeScript for .coffee assets and views
gem 'coffee-rails', '~> 4.1.0'

# See https://github.com/rails/execjs#readme for more supported runtimes
gem 'therubyracer', platforms: :ruby

# Use jquery as the JavaScript library
gem 'jquery-rails'

# Build JSON APIs with ease. Read more: https://github.com/rails/jbuilder
gem 'jbuilder', '~> 2.0'

# bundle exec rake doc:rails generates the API under doc/api.
gem 'sdoc', '~> 0.4.0', group: :doc

#user registration/auth
gem 'devise', '~> 3.4.1'

group :development do
  gem 'capistrano-rails'
end

group :development, :test do
  gem 'byebug'
  gem 'web-console', '~> 2.0'
  gem 'spring'
  #gem 'turbolinks'  #I'm not sold on turbolinks if you don't use it in prod too...
end

#group :production do
#  # Use Unicorn as the app server (in prod)
#  gem 'unicorn'
#end